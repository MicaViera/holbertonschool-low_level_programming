#include <stdio.h>
/**
 * main - print all the lowercase alphabet in reverse.
 * Return: 0
 */
int main(void)
{
	char rev;

	for (rev = 12; rev <= 97; rev--)
		putchar(rev);
	putchar('\n');
	return (0);
}
